this text file was added to the repository on the github website. I can use git pull (or fetch + merge) to put it on my machine locally now.

this text is only present on the "example" branch of the repository.
